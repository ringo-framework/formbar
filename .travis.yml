language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"

# command to install dependencies
install: "python setup.py -q install"

# command to run tests
script: python -m unittest discover formbar/tests
